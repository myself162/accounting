<div class="span12">
  <div class="group">
    <%= link_to("Edit", edit_bookkeeping_path(@bookkeeping), :class => "btn btn-mini btn-primary pull-right") %>
  </div>
  <h2> <%= @bookkeeping.title %> </h2>
  <p> <%= @bookkeeping.description %> </p>

</div>